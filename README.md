# Wahaj Hussain — Portfolio

AI-driven sales automation pipeline portfolio showcasing n8n case studies (AI Sales Employee, News-to-Social Pipeline, Gulf B2B Outreach).

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
3. Build for production:
   `npm run build`
